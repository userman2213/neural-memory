name: neural
version: 1.0.0
description: "Neural Memory — local semantic memory with knowledge graph, spreading activation, and auto-connections. Runs entirely offline."
requires_env: []
hooks:
  - on_session_end
  - on_memory_write
